ANTHROPIC_API_KEY=

# Concurrent Lean subprocess checks allowed (shared across all steps and
# ensemble candidates). Machine-dependent — tune to your own hardware, don't
# copy this value blindly. See config.py for the measured trade-off story.
LEAN_CONCURRENCY_LIMIT=2
